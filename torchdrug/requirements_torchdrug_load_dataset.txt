# TorchDrug Load Dataset Dependencies

# Core library
torchdrug>=0.2.0

# PyTorch (required backend)
torch>=1.10.0

# Chemistry toolkit for molecular processing
rdkit>=2022.03.1

# Graph library
networkx>=2.6.0

# Data processing
pandas>=1.3.0
numpy>=1.21.0

# Download and extraction
requests>=2.26.0
tqdm>=4.62.0  # Progress bars

# File handling
h5py>=3.1.0  # For HDF5 format datasets

# Installation:
# conda install -c conda-forge rdkit
# pip install torchdrug torch networkx pandas numpy requests tqdm h5py

# PyTorch installation (choose based on system):
# CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# System requirements:
# - Python >= 3.7
# - Disk space: 5-10GB for dataset cache
# - RAM: 4GB minimum, 8GB+ recommended
# - Internet connection for initial download
# - No GPU required for loading (only for training)
